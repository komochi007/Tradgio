# 任务 46 验收记录：Windows 迁移、升级与正式上线验收

日期：2026-06-16

## 1. 结论

任务 46 已完成一次上线门禁评估，最终结论为：暂缓上线。

本次自动化验收确认质量门禁、核心 E2E、备份恢复 E2E、localStorage 到 IndexedDB 迁移 E2E、PWA 更新和静态回滚均通过；并补充验证 PWA 更新与回滚后 IndexedDB 业务数据仍可读取。

由于目标 Windows Chrome/Edge 全新浏览器配置恢复、真实业务 UAT、用户确认和真实跨 schema 升级仍未完成，当前不得宣称正式数据迁移已完成，也不得进入正式上线。

## 2. 本次交付物

- PWA 发布回滚测试已增加业务数据保持断言：创建货品后，安全更新和静态回滚激活后仍能在货品列表读取同一条 IndexedDB 数据。
- 生产就绪清单已记录任务 46 的“暂缓上线”结论和剩余阻断项。
- 开发进度、生产路线图、README 和部署文档已同步当前运行状态。

## 3. 自动验收

- `npm run test:pwa`：通过，1 条 PWA 发布流程通过；覆盖离线启动、安全更新、编辑页阻断、静态回滚，以及更新/回滚后业务数据保持。
- `npm run quality`：通过，12 个测试文件、99 项测试通过，生产构建成功，依赖审计 0 个漏洞。
- `npm run test:e2e`：通过，5 条 Playwright 流程通过；覆盖核心业务链路、整机加密备份恢复、两次本地生产迁移演练和离线模板错误提示。

## 4. 版本与回滚记录

- 应用版本：`0.1.0`
- IndexedDB schema 版本：`1`
- 本次评估基线：本次任务 46 提交
- 当前线上 Pages 发布基线：`14a6ce8328a925998a6a3024143582bc2096bb90`
- 回滚策略：继续使用同一固定 Origin `https://komochi007.github.io/Tradgio/` 发布上一稳定兼容提交；若未来 schema 降级不兼容，必须发布兼容修复版，不能清除用户 IndexedDB。

## 5. 未通过阻断项

1. 目标 Windows Chrome 最新稳定版尚未使用全新浏览器配置导入 `.tradgio-backup` 并完成核心数据校验。
2. 目标 Windows Edge 最新稳定版尚未使用全新浏览器配置导入 `.tradgio-backup` 并完成核心数据校验。
3. 真实业务样本 UAT 尚未由用户确认，包含货品、往来单位、进货、出货、报价、合同、附件下载、搜索和导出结果。
4. 当前 schema 仍为 `1`，本次只能验证同 schema 发布更新与回滚不破坏 IndexedDB 数据；真正跨 schema 升级需在出现 schema `2` 生产候选版本时补证。
5. Origin 变更迁移流程尚未在真实新 Origin 上完成导出、导入和核对演练。

## 6. 当前风险

生产就绪清单仍存在阻断项，当前状态为“暂缓上线”。在补齐 Windows 恢复、真实 UAT、导出结果确认和跨 schema 升级证据前，不得把正式业务数据迁移到生产 Origin 作为唯一工作副本。
