# 生产化推进路线图

本文档是 Tradgio 从本地 MVP 推进到生产可上线状态的主要执行依据。

## 1. 使用规则

- 全局任务从 `任务 26` 延续编号；第三轮专项优化继续使用 `OPT3-*`，两者互不占号。
- 所有任务默认状态为“待开始”；只有完成交付物、验证和文档同步后才能更新状态。
- 任务按 `P0 -> P1 -> P2 -> P3` 推进，前一阶段的准入门槛未通过时，不进入后一阶段。
- 生产平台供应商不预设，由任务 34 形成 ADR 后锁定。
- 当前仍是单人业务系统，不扩展多人协作、复杂权限、财务和库存预警。

状态枚举：`待开始`、`进行中`、`待验收`、`已完成`、`阻塞`。

## 2. 阶段总览

| 阶段 | 目标 | 任务 | 准出条件 |
|---|---|---|---|
| P0 业务正确性 | 消除库存、隔离、编号和写入一致性风险 | 26-31 | 核心业务自动化回归全部通过 |
| P1 工程保障与技术决策 | 建立质量门禁并锁定生产方案 | 32-37 | ADR、数据模型、迁移和 Adapter 契约通过评审 |
| P2 生产能力接入 | 接入真实 Auth、数据库、存储和导出运行时 | 38-42 | 测试环境使用真实生产适配层跑通 |
| P3 发布准备 | 建立部署、监控、备份和上线验收 | 43-46 | 上线检查清单全部满足 |

## 3. P0 业务正确性

### 任务 26：自动化测试基础与库存回归用例

- 优先级：P0
- 状态：已完成
- 目标：建立可重复运行的测试基础，先用失败用例固定库存正确性问题。
- 实施内容：引入与 Vite/TypeScript 兼容的单元测试框架；增加 `npm run test` 和 CI 可用的非交互命令；覆盖进货、出货、负库存、同货品多明细、数量增减、删除明细和替换货品。
- 交付物：测试配置、库存计算测试、测试脚本和最小测试说明。
- 验收标准：现有正确场景通过；已知差额回算缺陷由测试稳定复现；测试不依赖浏览器 localStorage 全局状态。
- 依赖：任务 01-25、`OPT3-01` 至 `OPT3-09`。
- 验证方式：执行类型检查、单元测试和生产构建。
- 文档同步：更新 README 开发命令和本路线图状态。
- 完成记录：2026-06-10 完成。`npm run typecheck`、`npm run test`、`npm run build` 通过；库存测试共 11 项，6 项正确场景通过，5 项差额回算缺陷以预期失败用例稳定复现。

### 任务 27：修复库存差额回算

- 优先级：P0
- 状态：已完成
- 目标：确保进货单和出货单编辑后，库存只应用新旧单据影响的有符号差额。
- 实施内容：保留每个货品差额的正负方向；聚合同货品重复明细；正确处理数量增加、数量减少、删除明细、增加明细和替换货品；流水记录必须反映真实方向。
- 交付物：库存领域计算修复、应用服务调整和回归测试。
- 验收标准：进货减少会扣减库存，出货减少会恢复库存；替换货品会撤销旧货品影响并应用新货品影响；负库存仍只能由出货产生。
- 依赖：任务 26。
- 验证方式：库存单元测试全部通过，并手动验证至少一组进货改单和出货改单。
- 文档同步：更新 `specs/inventory-engine.md` 中的差额算法说明。
- 完成记录：2026-06-10 完成。库存回归测试 13 项全部通过；浏览器验证进货数量 `1000 -> 999` 后库存 `1000 -> 999`，出货数量 `4 -> 3` 后库存 `6 -> 7`，并已恢复样本数据。

### 任务 28：单据与库存写入一致性

- 优先级：P0
- 状态：已完成
- 目标：避免单据保存失败后库存已变化，或库存失败后单据已变化。
- 实施内容：为本地适配器建立统一提交编排和失败回滚；创建与编辑均保存前态并在后续步骤失败时恢复；定义未来远程数据库必须使用事务的契约；删除进货/出货单暂不开放，除非同步实现库存冲销。
- 交付物：统一保存用例、回滚机制、失败注入测试和事务边界说明。
- 验收标准：任一步骤失败后单据、库存流水和库存快照保持提交前状态；重复提交不会造成重复库存影响。
- 依赖：任务 27。
- 验证方式：对单据写入、流水写入和快照写入分别注入失败并验证状态。
- 文档同步：更新架构文档的数据流和事务约束。
- 完成记录：2026-06-10 完成。本地原子保存测试覆盖单据、流水、快照失败回滚，创建、编辑、失败重试、并发重复提交和不同请求串行化；进货/出货删除在实现库存冲销前保持关闭。

### 任务 29：账号业务数据隔离

- 优先级：P0
- 状态：已完成
- 目标：所有业务数据按当前账号隔离，不再由多个账号共享同一集合。
- 实施内容：业务实体增加 `accountId`；Repository 的读写必须携带账号上下文；库存、搜索、总览、编号生成和引用校验均限定当前账号；旧 localStorage 数据提供一次性版本化迁移，默认归属执行迁移时的当前账号。
- 交付物：账号归属模型、隔离 Repository、本地迁移器、迁移版本标记和隔离测试。
- 验收标准：账号 A 无法列表、详情读取、修改或搜索账号 B 的任何记录；迁移可重复执行且不会复制数据；无当前账号时禁止业务写入。
- 依赖：任务 28。
- 验证方式：使用两个账号执行完整 CRUD、搜索、库存和编号测试。
- 文档同步：更新领域模型、持久化说明和迁移说明。
- 完成记录：2026-06-10 完成。双账号测试覆盖基础资料 CRUD、进货/出货/报价/合同、统一搜索、库存和账号内编号；跨账号详情、修改、搜索和引用均被阻止；旧数据迁移可重复执行且不复制记录。

### 任务 30：单据编号生成加固

- 优先级：P0
- 状态：已完成
- 目标：避免删除记录、并发保存或异常数据导致编号重复。
- 实施内容：按账号、单据类型和月份读取已有最大流水号后递增；保存时再次检查唯一性；两位流水达到 `99` 时明确阻止创建并提示升级编号规则；生产数据库契约增加唯一约束。
- 交付物：统一编号生成器、重复检测、溢出处理和测试。
- 验收标准：删除中间记录后不会复用编号；历史编号乱序时仍取最大值；重复保存被拒绝；四类编号规则一致。
- 依赖：任务 29。
- 验证方式：覆盖删除、缺号、乱序、跨月、跨账号和 `99` 溢出场景。
- 文档同步：更新相关规格中的编号规则。
- 完成记录：2026-06-10 完成。四类编号统一按账号、类型和月份读取最大流水号后递增；Repository 写入时再次校验账号内唯一；报价和合同创建纳入串行保存；自动化测试覆盖删除、缺号、乱序、跨月、跨账号、重复写入、并发创建和 `99` 溢出，完整回归 4 个测试文件共 36 项通过。

### 任务 31：P0 综合回归验收

- 优先级：P0
- 状态：已完成
- 目标：形成进入生产技术选型和适配工作的质量门槛。
- 实施内容：执行库存、单据、账号隔离、编号、搜索和导出回归；核对本地数据迁移；记录缺陷、证据和结论。
- 交付物：P0 验收记录和更新后的生产就绪检查清单。
- 验收标准：P0 自动化测试全部通过；无阻断级业务缺陷；构建通过；账号隔离和失败回滚有明确证据。
- 依赖：任务 26-30。
- 验证方式：执行完整测试、构建和浏览器核心流程。
- 文档同步：更新开发进度；通过前不得启动任务 34 之后的实施。
- 完成记录：2026-06-11 完成。`npm run typecheck`、36 项自动化测试和生产构建通过；独立账号浏览器流程覆盖注册登录、基础资料、进货、出货、报价、合同、总览库存、统一搜索和会话恢复，控制台无错误；P0 本地业务正确性基线通过，完整证据见 `tasks/p0-acceptance-report.md`。

## 4. P1 工程保障与技术决策

### 任务 32：Lint、Format、CI 与依赖审计

- 优先级：P1
- 状态：已完成
- 目标：建立每次提交可重复执行的工程质量门禁。
- 实施内容：配置 lint、format check、类型检查、测试、构建和依赖审计命令；CI 对拉取请求和主分支执行；评估并处理 ExcelJS 依赖链中已知风险。
- 交付物：项目脚本、配置文件、CI 工作流和依赖风险处理记录。
- 验收标准：本地与 CI 命令一致；失败会阻止合并；无未处置的高危或严重漏洞，中危风险必须有升级、替换或接受说明。
- 依赖：任务 31。
- 验证方式：本地执行全部命令，并通过一次 CI 运行。
- 文档同步：更新 README 和贡献流程。
- 完成记录：2026-06-11 完成。`npm run quality` 在本地与 GitHub Actions 均通过，4 个测试文件共 36 项通过，依赖审计为 0 个漏洞；公开仓库启用 `main-quality-gate` Ruleset，严格要求 `quality` 状态检查。临时 PR #1 故意触发格式检查失败后，普通合并被仓库规则明确拒绝，验收分支随后已清理。

### 任务 33：核心流程 E2E 基线

- 优先级：P1
- 状态：待验收
- 目标：自动验证用户可见的核心业务链路。
- 实施内容：覆盖注册/登录、货品与往来单位、进货、出货、报价、合同、总览搜索和模板导出；测试数据独立且可清理；失败保留截图或追踪信息。
- 交付物：E2E 配置、测试用例、测试数据策略和 CI 集成。
- 验收标准：核心流程可在干净环境重复通过；测试之间无数据污染；关键失败有可定位证据。
- 依赖：任务 31、32。
- 验证方式：本地和 CI 各执行一次完整 E2E。
- 文档同步：更新测试说明和上线清单。
- 验收记录：2026-06-11 本地 `npm ci`、`npm run quality` 和 `npm run test:e2e` 通过；Playwright 核心流程覆盖注册登录、基础资料、进货、出货、报价、合同、库存、统一搜索、会话恢复和模板 Excel 下载，独立浏览器冒烟验证控制台无错误。待 GitHub Actions E2E 通过后完成验收。

### 任务 34：生产平台技术选型 ADR

- 优先级：P1
- 状态：待开始
- 目标：锁定 Auth、PostgreSQL、对象存储、部署平台和导出运行时。
- 实施内容：比较至少两套可行方案；按中国大陆访问稳定性、成本、开发复杂度、事务能力、备份恢复、供应商锁定和运维负担评分；记录选型、弃选原因和退出方案。
- 交付物：生产平台 ADR、成本估算、环境拓扑和风险清单。
- 验收标准：所有生产能力有唯一选定方案；明确开发/测试/生产环境；明确密钥管理、网络约束和服务降级策略。
- 依赖：任务 31；可与任务 32-33并行调研，不得提前实施供应商 SDK。
- 验证方式：完成方案评审并记录结论日期。
- 文档同步：更新架构文档和环境变量方向。

### 任务 35：生产数据模型与 API 契约

- 优先级：P1
- 状态：待开始
- 目标：在编码前明确生产数据结构、事务和权限边界。
- 实施内容：定义账号、基础资料、单据主从表、库存流水/快照、合同附件元数据的 schema；定义唯一键、外键、软硬删除策略、金额精度、时间字段和账号级访问规则；定义 API 输入输出与错误模型。
- 交付物：数据库设计、API 契约、迁移初稿和权限矩阵。
- 验收标准：单据与库存可在同一事务提交；编号有数据库唯一约束；附件二进制不进入数据库；服务端强制账号隔离。
- 依赖：任务 34。
- 验证方式：契约评审和关键业务场景走查。
- 文档同步：更新架构文档及对应 specs。

### 任务 36：数据迁移与回滚方案

- 优先级：P1
- 状态：待开始
- 目标：保证现有本地数据可验证地迁移到生产数据层。
- 实施内容：定义本地导出格式、版本号、校验摘要、账号映射、附件处理、预检查、导入顺序、幂等键、失败重试和回滚；禁止直接读取用户浏览器数据进行隐式迁移。
- 交付物：迁移设计、数据包 schema、演练步骤和回滚手册。
- 验收标准：同一数据包重复导入不会重复建单或重复影响库存；迁移前后记录数、金额和库存可核对；失败可安全重试或整体回滚。
- 依赖：任务 29、35。
- 验证方式：使用脱敏样本完成至少一次迁移演练设计评审。
- 文档同步：更新上线清单和用户数据迁移说明。

### 任务 37：生产 Adapter 与环境配置契约

- 优先级：P1
- 状态：待开始
- 目标：锁定页面层与生产平台之间的稳定边界。
- 实施内容：完善 Auth、Data、File、Export Adapter；明确账号上下文、分页/查询、上传下载、超时、重试、错误映射和取消语义；形成 `.env.example` 字段清单，客户端不得包含服务端密钥。
- 交付物：接口契约、环境变量清单、错误模型和 Adapter 测试替身。
- 验收标准：页面和领域层不直接依赖供应商 SDK；本地与远程 Adapter 可按配置切换；缺失配置时启动即给出明确错误。
- 依赖：任务 34、35。
- 验证方式：契约测试和模拟 Adapter 演练。
- 文档同步：更新持久化注册表、README 和架构文档。

## 5. P2 生产能力接入

### 任务 38：真实 Auth 接入

- 优先级：P2
- 状态：待开始
- 目标：替换本地明文密码和伪会话。
- 实施内容：按 ADR 接入真实注册、登录、退出和会话恢复；服务端身份作为账号归属唯一来源；处理会话过期、重复用户名、网络失败和未授权跳转。
- 交付物：生产 Auth Adapter、配置、集成测试和本地 Auth 退场说明。
- 验收标准：浏览器不保存明文密码；刷新可恢复有效会话；过期会话安全退出；账号身份无法由客户端伪造。
- 依赖：任务 34、37。
- 验证方式：Auth 集成测试和双账号隔离 E2E。
- 文档同步：更新 Auth spec、运行说明和上线清单。

### 任务 39：远程数据库与 Repository 接入

- 优先级：P2
- 状态：待开始
- 目标：将业务数据和库存真相迁移到生产 PostgreSQL。
- 实施内容：实施 schema 和迁移；实现远程 Repository；服务端校验账号归属；单据与库存使用事务；查询、总览和编号生成切换到远程数据源。
- 交付物：数据库迁移、远程 Data Adapter、权限策略和集成测试。
- 验收标准：所有 CRUD、搜索和库存链路使用远程数据；事务失败不留半成品；数据库约束阻止跨账号和重复编号写入。
- 依赖：任务 35、37、38。
- 验证方式：测试环境集成测试、故障注入和双账号 E2E。
- 文档同步：更新数据库运行与恢复说明。

### 任务 40：合同对象存储接入

- 优先级：P2
- 状态：待开始
- 目标：将合同二进制从 localStorage 迁移到对象存储。
- 实施内容：实现上传、下载和删除 Adapter；数据库仅保存文件名、大小、类型、存储键和时间；使用受控下载权限；上传失败不得创建半成品合同，删除失败需可重试和审计。
- 交付物：File Adapter、附件元数据迁移、权限配置和测试。
- 验收标准：大于 localStorage 容量的合法文件可上传下载；账号无法访问他人附件；失败不会留下不可追踪孤儿文件。
- 依赖：任务 35、37-39。
- 验证方式：多格式、大小边界、权限和失败恢复测试。
- 文档同步：更新合同 spec、存储策略和上线清单。

### 任务 41：导出能力生产化

- 优先级：P2
- 状态：待开始
- 目标：按 ADR 决定并实现可维护的生产导出运行方式。
- 实施内容：若保留客户端导出，则延迟加载 ExcelJS 和模板并控制包体；若改为服务端，则实现受鉴权的导出 Adapter、超时和文件返回；两种方案都保持页面调用接口不变。
- 交付物：生产 Export Adapter、性能优化、错误处理和导出回归测试。
- 验收标准：导出失败不影响单据；模板内容和样式保持验收基线；首屏不加载非必要导出依赖；账号只能导出自己的单据。
- 依赖：任务 34、37、39。
- 验证方式：真实样本导出、权限、性能和失败场景测试。
- 文档同步：更新导出 spec、架构和运行说明。

### 任务 42：本地数据迁移工具

- 优先级：P2
- 状态：待开始
- 目标：实现从现有本地 MVP 到生产数据层的可审计迁移。
- 实施内容：实现本地数据导出、服务端预检查、导入、进度和结果报告；附件单独上传并关联；使用幂等键；提供只校验不写入模式和失败重试。
- 交付物：迁移工具、数据包版本、迁移报告和操作手册。
- 验收标准：脱敏样本迁移前后实体数量、金额、库存和附件一致；重复执行无重复数据；失败报告可定位到具体记录。
- 依赖：任务 36、39-41。
- 验证方式：测试环境至少完成两次完整迁移演练。
- 文档同步：更新迁移手册、上线清单和进度记录。

## 6. P3 发布准备

### 任务 43：生产适配层自动化回归

- 优先级：P3
- 状态：待开始
- 目标：用真实生产适配层验证完整业务行为。
- 实施内容：扩展 Auth、数据库、存储、导出的集成测试；覆盖权限、事务、并发编号、迁移数据和网络失败；E2E 指向测试环境。
- 交付物：生产适配层测试套件、测试报告和缺陷记录。
- 验收标准：P0 核心用例在远程环境全部通过；无跨账号访问；无事务一致性缺陷；关键外部依赖失败有用户可理解反馈。
- 依赖：任务 38-42。
- 验证方式：CI 集成测试和测试环境 E2E。
- 文档同步：更新生产就绪检查清单。

### 任务 44：测试环境与部署流水线

- 优先级：P3
- 状态：待开始
- 目标：建立可重复、可回滚的测试和生产部署流程。
- 实施内容：配置测试/生产环境、环境变量、数据库迁移步骤、静态资源部署、健康检查和回滚；主分支发布必须经过质量门禁和人工确认。
- 交付物：部署配置、流水线、环境说明和回滚步骤。
- 验收标准：测试环境可从零部署；失败发布不会破坏现有版本；上一稳定版本可恢复；生产密钥不进入仓库或构建日志。
- 依赖：任务 32、34、37、43。
- 验证方式：完成一次测试环境部署和一次回滚演练。
- 文档同步：新增部署与运行文档，更新 README。

### 任务 45：监控、备份与恢复

- 优先级：P3
- 状态：待开始
- 目标：上线后能够发现故障并恢复业务数据。
- 实施内容：接入前端错误、服务端错误和关键任务日志；配置数据库与对象存储备份；定义告警联系人、严重级别、恢复目标和恢复演练频率；日志不得记录密码、令牌或附件内容。
- 交付物：监控面板、告警规则、备份策略、恢复手册和演练记录。
- 验收标准：测试错误能触发告警；数据库和附件均有备份；从备份恢复后通过核心数据校验；敏感信息未进入日志。
- 依赖：任务 39、40、44。
- 验证方式：告警测试和一次恢复演练。
- 文档同步：更新运维手册和上线清单。

### 任务 46：UAT 与正式上线验收

- 优先级：P3
- 状态：待开始
- 目标：使用真实业务样本确认系统可正式上线。
- 实施内容：冻结上线版本；完成迁移演练、真实业务 UAT、性能和兼容性检查；确认回滚负责人和窗口；逐项关闭生产就绪清单。
- 交付物：UAT 记录、上线审批结论、迁移报告、回滚确认和版本标记。
- 验收标准：`tasks/production-readiness-checklist.md` 阻断项全部通过；无未关闭的 P0/P1 缺陷；用户确认关键单据与导出结果；上线和回滚步骤均已演练。
- 依赖：任务 43-45。
- 验证方式：按生产就绪清单逐项留证并形成最终结论。
- 文档同步：更新 README、开发进度、发布记录和当前运行状态。

## 7. 当前启动顺序

立即执行顺序：`任务 26 -> 27 -> 28 -> 29 -> 30 -> 31`。

P0 完成前只允许开展任务 34 的资料调研，不实施真实后端、对象存储或部署平台接入。
